/* Plugin Template generated by Pawn Studio */

#include <sourcemod>
#include <CodD0_engine>
#include <CodD0/skills/CodD0_skill_damage>

public Plugin myinfo =  {
	name = "CodD0 Perk: Armoured vest",
	author = "d0naciak",
	description =  "Armoured vest",
	version = "1.0",
	url = "d0naciak.pl"
}

int g_perkID;
bool g_hasPerk[MAXPLAYERS + 1];

public void OnPluginStart() {
	g_perkID = CodD0_RegisterPerk("Pancerna Zbroja", "Obrażenia z wybuchów są redukowane do LW%", 15, 60);
}

public void OnPluginEnd() {
	CodD0_UnregisterPerk(g_perkID);
}

public Action CodD0_PerkChanged(int client, int perkID, int perkValue) {
	if(g_perkID != perkID && g_hasPerk[client]) {
		CodD0_SetClientExploRedDmgMultiplier(client, CodD0_SkillSlot_Perk, 1.0);
		g_hasPerk[client] = false;
	}
	
	return Plugin_Continue;
}

public void CodD0_PerkChanged_Post(int client, int perkID, int perkValue) {
	if (perkID == g_perkID) {
		CodD0_SetClientExploRedDmgMultiplier(client, CodD0_SkillSlot_Perk, 1.0 - (float(perkValue) * 0.01));
		g_hasPerk[client] = true;
	}
}