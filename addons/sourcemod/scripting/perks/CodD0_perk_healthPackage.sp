/* Plugin Template generated by Pawn Studio */

#include <sourcemod>
#include <CodD0_engine>

public Plugin myinfo =  {
	name = "CodD0 Perk: Health Package",
	author = "d0naciak",
	description =  "Health Package",
	version = "1.0",
	url = "d0naciak.pl"
}

int g_perkID;

public void OnPluginStart() {
	g_perkID = CodD0_RegisterPerk("Pakiet Zdrowia", "+LW do statystyki zdrowia", 15, 45);
}

public OnPluginEnd() {
	CodD0_UnregisterPerk(g_perkID);
}

public void CodD0_PerkChanged_Post(int client, int perkID, int perkValue) {
	static int perkLastValue[MAXPLAYERS + 1];

	if(perkID == g_perkID) {
		CodD0_SetClientBonusStatsPoints(client, HEALTH_PTS, CodD0_GetClientBonusStatsPoints(client, HEALTH_PTS) + perkValue - perkLastValue[client]);
		perkLastValue[client] = perkValue;
	} else if(perkLastValue[client]) {
		CodD0_SetClientBonusStatsPoints(client, HEALTH_PTS, CodD0_GetClientBonusStatsPoints(client, HEALTH_PTS) - perkLastValue[client]);
		perkLastValue[client] = 0;
	}
}